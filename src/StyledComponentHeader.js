import React from 'react';
import styled from 'styled-components';

const StyledHeader = styled.h2`
  color: mediumseagreen;
`
export default () => {
  return(
      <StyledHeader>Styled Component</StyledHeader>
  )
}

